name: nginx-ingress-update

scms:
  default2:
    kind: github
    spec:
      owner: "{{ .github.owner }}"
      repository: "{{ .github.repository }}"
      username: "{{ .github.username }}"
      token: '{{ requiredEnv "UPDATECLI_GITHUB_TOKEN" }}'
      branch: "{{ .github.branch }}"

sources:
  ingress2:
    kind: helmchart
    spec:
      url: "https://kubernetes.github.io/ingress-nginx"
      name: "ingress-nginx"

targets:
  publicIngress2:
    name: "ingress-nginx"
    kind: helmchart
    sourceid: ingress2
    scmid: default2
    spec:
      name: "charts/ingress-nginx2"
      file: "Chart.yaml"
      key: "$.dependencies[0].version"

actions:
  nodesAmis2:
    kind: github/pullrequest
    title: Update nginx 2 `{{ source "ingress2" }}`
    scmid: default2
    spec:
      draft: true
      labels:
      - eks
      - dependencies